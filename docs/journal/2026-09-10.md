# RainWatch development journal — 2026-09-10

This is the detailed implementation record for the day. `progress.html` remains the short visual dashboard; dated Markdown files hold reproducible technical detail.

## Project foundation

### What

Created the initial local-first RainWatch web application foundation in the canonical project directory.

### How

- Canonical project directory: `C:\D_Data\AI-Projects\rainwatch`
- Local Git repository using the `main` branch
- React 19, TypeScript 6, and Vite 8
- Static client-side architecture with no backend or database
- npm dependencies installed locally under the ignored `node_modules` directory
- GitHub remote: `https://github.com/kclee/rainwatch.git`

### Verification

- `npm run build` passed.
- `npm run lint` passed.
- The local `main` branch and GitHub `main` matched after the initial push.
- `node_modules` and production build output are excluded from Git.

### Files

- `.gitignore`
- `.oxlintrc.json`
- `README.md`
- `index.html`
- `package.json`
- `package-lock.json`
- `src/App.tsx`
- `src/App.css`
- `src/index.css`
- `src/main.tsx`
- TypeScript and Vite configuration files

### Git

Commit: `Initial RainWatch React and Vite scaffold`

## Interactive MapLibre basemap

### What

Implemented the first version 0.1a product milestone: a responsive interactive map that supports mouse, keyboard, and touch-oriented MapLibre controls.

### How

- Added MapLibre GL JS 6.9.0 as a project-local dependency.
- Added `WeatherMap`, which owns the MapLibre instance and cleans it up when React unmounts the component.
- Added zoom controls, a scale indicator, and compact provider/data attribution.
- Added a `ResizeObserver` so the map canvas tracks its responsive container.
- Excluded MapLibre from Vite's development dependency optimizer because its worker module is loaded dynamically; this avoids a missing optimized-worker diagnostic during hot reload.
- Added a visible loading state and a graceful message for basemap loading errors.
- Isolated the style URL, default view, and zoom limits in `src/config/map.ts`.
- Selected the public OpenFreeMap Liberty style for development. Its attribution remains visible on the map.
- Added the optional `VITE_BASEMAP_STYLE_URL` override so the provider can be replaced without editing the UI component.
- Used a general continental United States fallback view; no personal location is hard-coded.

### Verification

- `npm run build` passed with the expected MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- The app was opened in a local browser at `http://127.0.0.1:5173/`.
- Visual inspection confirmed that the basemap, zoom buttons, scale, and required attribution render.
- Browser inspection found no console errors or warnings from the application.
- Development-server startup and reload completed without the earlier optimized-worker diagnostic.
- A browser check caught and corrected a stylesheet specificity conflict that initially left the map container at zero height.
- The OpenFreeMap style endpoint returned HTTP 200 and valid style metadata.

### Files

- `src/components/WeatherMap.tsx`
- `src/config/map.ts`
- `src/App.tsx`
- `src/App.css`
- `README.md`
- `package.json`
- `package-lock.json`
- `progress.html`

### Git

Commit: `Add interactive MapLibre basemap`

## Browser geolocation and location marker

### What

Added explicit, privacy-conscious browser geolocation. The map remains fully usable at its general fallback view until the user chooses **Use my location**.

### How

- Added `useGeolocation`, which owns permission requests and maps browser results into small application states.
- Added friendly handling for unsupported geolocation, permission denial, unavailable positions, timeouts, and unexpected errors.
- Requested cached, standard-accuracy positions with a ten-second timeout; high-accuracy GPS is not forced.
- Added a clear MapLibre marker when coordinates are available.
- Centered and zoomed the map near the returned position without a RainWatch backend or persistent storage.
- Changed the control to **Return to my location** after success; requesting again updates and recenters the marker.
- Kept the control at least 44 pixels tall for touch use.
- Kept precise coordinates in React memory only for the current page session. RainWatch has no backend or location persistence. As with ordinary interactive maps, the basemap provider receives tile requests for the area currently in view.
- Preserved the general continental United States fallback and normal map interaction when permission is denied or unavailable.

### Verification

- `npm run build` passed with the existing MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- Browser inspection confirmed the location control, privacy note, full-height map, and absence of a marker before permission is requested.
- No application console errors or warnings were reported.
- At a 390 × 844 viewport, the location button remained 44 pixels tall, the map remained usable, and the document had no horizontal overflow.
- The test did not click the location button or grant location permission, so no real location was accessed or transmitted. Permission-granted behavior requires user verification in their normal browser.

### Files

- `src/hooks/useGeolocation.ts`
- `src/types/weather.ts`
- `src/components/WeatherMap.tsx`
- `src/App.tsx`
- `src/App.css`
- `README.md`
- `progress.html`

### Git

Commit: `Add browser geolocation controls`

## First RainViewer radar frame

### What

Added the first real radar overlay while deliberately limiting the UI to one frame. RainWatch fetches the available historical frames and displays the newest one above the basemap.

### How

- Added the small `RadarProvider` interface and provider-neutral `RadarFrame` type.
- Added `RainViewerRadarProvider`, which owns the metadata endpoint, response validation, historical-frame filtering, tile URL construction, and attribution.
- Used only `radar.past`; the app does not read or assume the existence of future/nowcast frames.
- Kept frames within approximately two hours of the newest available historical frame.
- Used RainViewer's 256-pixel raster tiles, Universal Blue color scheme 2, smoothing, and separate snow colors.
- Declared RainViewer's documented maximum native tile zoom of 7 and allowed MapLibre to overzoom the raster at closer map levels.
- Inserted the radar layer below the basemap's first symbol layer so labels remain readable.
- Set an initial radar opacity of 70 percent, aligned with the later control's five-percent increments.
- Added a ten-second metadata timeout, invalid-response handling, an empty-frame state, and a radar-tile failure message that leaves the basemap usable.
- Displayed the selected radar frame in the user's local time zone.

### Verification

- RainViewer's official metadata endpoint returned API version 2.0 with 13 past frames at ten-minute intervals and zero nowcast frames during the check.
- RainViewer's documented tile host and historical frame paths were used to construct the current raster template.
- `npm run build` passed with the existing MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- Live browser inspection confirmed the newest radar frame, local timestamp, RainViewer attribution, basemap labels, location control, and absence of application console errors.
- At 390 × 844, the radar status remained compact, the location control stayed 44 pixels tall, and the document had no horizontal overflow.
- No location permission was requested during radar verification.

### Files

- `src/services/radar/RadarProvider.ts`
- `src/services/radar/RainViewerRadarProvider.ts`
- `src/hooks/useRadarFrames.ts`
- `src/types/weather.ts`
- `src/components/WeatherMap.tsx`
- `src/App.tsx`
- `src/App.css`
- `README.md`
- `progress.html`

### Git

Commit: `Display latest RainViewer radar frame`

## Historical radar timeline

### What

Made every available historical radar frame selectable through a range control with explicit previous and next actions. The selected local time and frame position remain visible while the radar layer updates.

### How

- Added a dedicated `Timeline` component with provider-neutral `RadarFrame` inputs.
- Started at the newest available historical frame.
- Added Previous and Next buttons with disabled boundaries at the oldest and newest frames.
- Added a native range input so mouse, touch, and keyboard users can scrub the available history.
- Exposed the selected local weekday, time, time zone, and frame count.
- Added an accessible value description such as `Thu, 1:40 PM CDT, frame 12 of 13`.
- Reused the existing MapLibre raster source and changed its tile template through `setTiles`, avoiding layer removal and preserving basemap labels and attribution.
- Kept playback out of this milestone so automatic advancement can be implemented and tested independently.

### Verification

- `npm run build` passed with the existing MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- Live RainViewer metadata supplied 13 frames at ten-minute intervals during the browser check.
- Previous changed the selected frame from 13 of 13 at 1:50 PM CDT to 12 of 13 at 1:40 PM CDT; Next returned to the latest frame.
- Keyboard Home selected frame 1 of 13 and disabled Previous; End returned to frame 13 of 13.
- Browser inspection reported no application console errors or warnings.
- At 390 × 844, the timeline matched the viewport width, the slider used the available width, both action buttons remained at least 44 pixels tall, and the document had no horizontal overflow.
- No location permission was requested during timeline verification.

### Files

- `src/components/Timeline.tsx`
- `src/components/WeatherMap.tsx`
- `src/App.tsx`
- `src/App.css`
- `README.md`
- `progress.html`

### Git

Commit: `Add historical radar timeline`

## Looping radar playback

### What

Added explicit Play and Pause controls that animate the available historical radar frames and loop continuously until paused.

### How

- Added `isPlaying` state at the application level beside the selected frame index.
- Added one interval that advances the frame every 800 milliseconds.
- Isolated the interval in `src/config/radar.ts` so playback speed is easy to tune later.
- Looped from the newest frame back to the oldest with modular index arithmetic.
- Cleared the interval whenever playback pauses, the frame count changes, or the application unmounts.
- Disabled playback when fewer than two frames are available.
- Used one toggle button whose label and pressed state expose Play or Pause clearly.
- Suppressed repeated live-region time announcements during automatic playback while preserving announcements for manual selection.
- Kept manual Previous, Next, slider, Home, and End navigation available alongside playback.

### Verification

- `npm run build` passed with the existing MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- Browser playback began at frame 13 of 13, looped to frame 1, then advanced to frame 2 on the next interval.
- After Pause at frame 2, the selected frame remained unchanged for longer than one playback interval.
- The toggle label changed from Play to Pause while active and returned to Play when stopped.
- Browser inspection reported no application console errors or warnings.
- At 390 × 844, Play, Previous, and Next were all 44 pixels tall, the timeline and slider fit the viewport, and the document had no horizontal overflow.
- No location permission was requested during playback verification.

### Files

- `src/config/radar.ts`
- `src/components/Timeline.tsx`
- `src/App.tsx`
- `src/App.css`
- `README.md`
- `progress.html`

### Git

Commit: `Add looping radar playback`

## Radar opacity and version 0.1a cleanup

### What

Completed the requested version 0.1a feature set with adjustable radar opacity and a final responsive/error-state review. The package prerelease version is now `0.1.0-alpha.1`.

### How

- Added a radar opacity range from 0 to 100 percent in five-percent increments.
- Selected 70 percent as the default so roads, labels, and geography remain visible below the radar.
- Displayed the current percentage beside the control and through an accessible value description.
- Updated the existing MapLibre radar layer with `setPaintProperty`; the map and raster source are not reconstructed when opacity changes.
- Kept opacity independent of frame selection and playback.
- Added an active-request guard so an aborted RainViewer request cannot update React state after unmount.
- Removed the global 320-pixel minimum width that caused scrollbar-related horizontal overflow at the smallest supported layout.
- Stacked the narrow header and kept its location action on one line.
- Kept location feedback visible on narrow screens after a request while hiding only the non-actionable idle note.
- Preserved 44-pixel minimum action heights for touch use.

### Verification

- `npm run build` passed with the documented MapLibre bundle-size advisory.
- `npm run lint` passed with no reported issues.
- Browser checks confirmed the opacity label and control at 0, 40, 70, and 100 percent.
- Playback advanced from frame 13 to frame 1 while the selected 40-percent opacity remained unchanged.
- Browser inspection reported no application console errors or warnings.
- At a 320 × 568 viewport, the header, map, timeline, playback controls, and opacity slider fit the actual 305-pixel content width with zero horizontal overflow.
- All timeline actions and the location action remained at least 44 pixels tall.
- The final review confirmed implemented messages for geolocation denial, unavailable position, timeout, unsupported geolocation, RainViewer request failure, missing radar frames, and radar tile errors.
- Real location permission was not granted during automated testing and remains a manual acceptance item.

### Files

- `src/config/radar.ts`
- `src/components/Timeline.tsx`
- `src/components/WeatherMap.tsx`
- `src/hooks/useRadarFrames.ts`
- `src/App.tsx`
- `src/App.css`
- `src/index.css`
- `package.json`
- `package-lock.json`
- `README.md`
- `progress.html`

### Git

Commit: `Complete RainWatch 0.1a controls`

## Documentation convention

RainWatch follows the useful parts of Image Finder's documentation pattern:

- `README.md` explains the product, setup, architecture, and limitations.
- `progress.html` provides a concise visual status dashboard.
- `docs/journal/YYYY-MM-DD.md` records implementation methods, verification evidence, affected files, and commit subjects.

Documentation should be updated with each meaningful milestone rather than every tiny edit.

## Next

1. Run `npm run dev` and open the local URL in a normal browser.
2. Grant location permission and confirm that the position marker appears near the device.
3. Pan away, then confirm that **Return to my location** recenters the map.
4. Exercise the radar timeline, playback, and opacity control together.
5. After user acceptance, consider PWA installation support as the next milestone.
